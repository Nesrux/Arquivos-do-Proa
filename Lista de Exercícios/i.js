// Lê os valores
let A = parseInt(prompt("Digite o valor de A:"));
let B = parseInt(prompt("Digite o valor de B:"));

// Calcula o quadrado da diferença (A - B)²
let resultado = Math.pow(A - B, 2);

// Mostra o resultado
alert("Resultado do quadrado da diferença: " + resultado);